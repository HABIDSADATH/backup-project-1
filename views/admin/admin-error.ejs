

<%-include("../../views/partials/admin/header")%>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #f6f8fb 0%, #f1f4f9 100%);
            padding: 20px;
        }

        .error-container {
            text-align: center;
            max-width: 600px;
            padding: 40px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            animation: fadeIn 0.6s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .error-heading {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .number {
            font-size: 96px;
            font-weight: bold;
            background: linear-gradient(135deg, #2d3748 0%, #4a5568 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.1);
            animation: bounce 2s infinite;
        }

        .number:nth-child(2) {
            animation-delay: 0.2s;
        }

        .number:nth-child(3) {
            animation-delay: 0.4s;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        h1 {
            font-size: 28px;
            color: #2d3748;
            margin-bottom: 15px;
            font-weight: 600;
        }

        p {
            color: #718096;
            margin-bottom: 35px;
            line-height: 1.6;
            font-size: 16px;
        }

        .home-button {
            display: inline-block;
            padding: 14px 32px;
            background: linear-gradient(135deg, #fc6f75 0%, #ff8087 100%);
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(252, 111, 117, 0.2);
        }

        .home-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(252, 111, 117, 0.3);
        }

        .home-button:active {
            transform: translateY(0);
        }

        footer {
            margin-top: 50px;
            color: #a0aec0;
            font-size: 14px;
        }

        footer a {
            color: #4a5568;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: #fc6f75;
        }

        @media (max-width: 480px) {
            .error-container {
                padding: 30px 20px;
            }

            .number {
                font-size: 72px;
            }

            h1 {
                font-size: 24px;
            }

            p {
                font-size: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-heading">
            <span class="number">4</span>
            <span class="number">0</span>
            <span class="number">4</span>
        </div>
        <h1>Page not found</h1>
        <p>The page you are looking for was moved, removed or might never existed.</p>
        <a href="/admin" class="home-button">Go to homepage</a>
    </div>


    <%-include("../../views/partials/admin/footer")%>
